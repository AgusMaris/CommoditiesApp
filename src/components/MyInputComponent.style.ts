import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  textInputContainer: {
    paddingHorizontal: 30,
    flexDirection: 'row',
    alignItems: 'center',
  },
  textInput: {
    borderColor: '#aaa',
    borderWidth: 1,
    borderRadius: 10,
    padding: 5,
    flex: 3,
  },
})
